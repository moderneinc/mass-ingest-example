# Bind a cloud identity here (EKS Pod Identity, IRSA, GKE Workload Identity) to reach the
# artifact store without a key.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mass-ingest
  namespace: mass-ingest
