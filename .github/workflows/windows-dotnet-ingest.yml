name: Windows .NET mass-ingest

on:
  workflow_dispatch:
    inputs:
      repos_csv:
        description: "repos.csv to ingest (defaults to the bundled .NET sample)"
        required: false
        default: test/windows-dotnet-repos.csv
  # TEMPORARY: validate the Windows .NET path on the PR before this workflow lands on
  # main (workflow_dispatch is not dispatchable until then). Revert to on-demand-only
  # before merge.
  pull_request:
    branches: [ main ]
    paths:
      - 'publish.ps1'
      - 'moderne.yml'
      - 'nuget/**'
      - 'test/windows-dotnet-repos.csv'
      - '.github/workflows/windows-dotnet-ingest.yml'

concurrency:
  group: windows-dotnet-ingest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ingest:
    runs-on: windows-latest
    timeout-minutes: 60
    env:
      REPOS_CSV: ${{ github.event.inputs.repos_csv || 'test/windows-dotnet-repos.csv' }}
    steps:
      - uses: actions/checkout@v4

      # Moderne's dotnet build step needs .NET SDK 10.0+ and the NuGet CLI on PATH.
      # windows-latest already ships MSBuild / VS Build Tools (needed for .NET Framework
      # target frameworks); no Mono is required on Windows.
      - name: Set up .NET SDK 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Set up NuGet CLI
        uses: nuget/setup-nuget@v2

      - name: Install the Moderne CLI
        shell: pwsh
        run: |
          irm https://app.moderne.io/cli/windows | iex
          mod --version
          # Each workflow step runs in a fresh shell, so the installer's PATH update is not
          # visible to later steps. Persist the CLI's bin directory via $GITHUB_PATH.
          Split-Path (Get-Command mod).Source | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Report the .NET build toolchain
        shell: pwsh
        run: |
          dotnet --info
          Write-Host "nuget: $((Get-Command nuget).Source)"
          nuget help | Select-Object -First 1

      - name: Sync and build .NET LSTs
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $clone = "$env:RUNNER_TEMP\moderne\repos"
          New-Item -Type Directory $clone -Force | Out-Null

          mod git sync csv $clone $env:REPOS_CSV --with-sources
          mod build $clone --no-download
          "CLONE_DIR=$clone" | Out-File -FilePath $env:GITHUB_ENV -Append
          # Forward-slash copy for actions/upload-artifact (its globbing rejects backslashes)
          "LST_OUT=$($clone -replace '\\','/')" | Out-File -FilePath $env:GITHUB_ENV -Append

      # Assert the build produced real LST build output, not a silent no-op. Also prints
      # the .moderne tree so the exact artifact layout is visible in the run log.
      # TODO(stronger): assert type attribution is present on the C# LSTs. `FindMissingTypes`
      # is Java-only; confirm a rewrite-csharp diagnostic (or a typed-node data table) exists
      # and fail the job when C# LSTs come back un-attributed.
      - name: Assert LSTs were produced
        shell: pwsh
        run: |
          Write-Host "== .moderne tree under $env:CLONE_DIR =="
          $moderne = Get-ChildItem -Path $env:CLONE_DIR -Recurse -Force -File -ErrorAction SilentlyContinue |
                     Where-Object { $_.FullName -match '\\\.moderne\\' }
          $moderne | ForEach-Object { Write-Host ("{0,12}  {1}" -f $_.Length, $_.FullName) }

          # A successful .NET build writes a build.log plus LST artifacts under
          # .moderne\build\<timestamp>\; require at least one non-empty artifact there
          # that is not the log itself.
          $artifacts = $moderne | Where-Object {
            $_.FullName -match '\\\.moderne\\build\\' -and $_.Name -ne 'build.log' -and $_.Length -gt 0
          }
          if (-not $artifacts) {
            Write-Error "No LST build artifacts found under $env:CLONE_DIR (build produced no LSTs)"
            exit 1
          }
          Write-Host "Produced $($artifacts.Count) LST build artifact(s)."

      - name: Upload build logs and LSTs for inspection
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-dotnet-lsts
          path: ${{ env.LST_OUT }}/**/.moderne/**
          if-no-files-found: warn
          retention-days: 7
