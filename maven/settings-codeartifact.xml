<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
    <!--
      Maven settings for resolving dependencies through AWS CodeArtifact during the build.
      When CODEARTIFACT_DOMAIN is set, publish.sh renders this template with the current
      token into $HOME/.m2/settings-codeartifact.xml (re-rendered on every token refresh,
      since the running CLI cannot see a changed environment variable) and registers that
      file. ${env.PUBLISH_URL} is the CodeArtifact endpoint.

      The catch-all mirror routes all resolution through CodeArtifact, which must therefore
      have an upstream connection for every external repository the build needs.
    -->
    <servers>
        <server>
            <id>codeartifact</id>
            <username>aws</username>
            <password>${env.CODEARTIFACT_AUTH_TOKEN}</password>
        </server>
    </servers>
    <mirrors>
        <mirror>
            <id>codeartifact</id>
            <name>AWS CodeArtifact</name>
            <url>${env.PUBLISH_URL}</url>
            <mirrorOf>*</mirrorOf>
        </mirror>
    </mirrors>
</settings>
