# ==============================================================================
# Mass-ingest configuration
# Edit this file and save as '.env' before running 'docker compose up'
# ==============================================================================

# Required: Artifact repository configuration
# Choose one of the storage options below:

# ==============================================================================
# Option A: S3 Storage
# ==============================================================================
# PUBLISH_URL=s3://your-bucket/path/to/lsts/
# S3_PROFILE=default                                    # Optional: AWS profile name
# S3_REGION=us-west-2                                   # Optional: For cross-region bucket access
# S3_ENDPOINT=https://minio.example.com                 # Optional: For S3-compatible services
# AWS_REGION=us-east-1                                  # Optional: Explicit region for AWS SDK
#
# Note: On EC2 with IAM roles, the AWS SDK gets credentials from the instance
# metadata service (IMDS). Docker adds a network hop, so you must set the IMDSv2
# hop limit to 2. See 1-quickstart/README.md for details.

# ==============================================================================
# Option B: Maven/Artifactory Repository
# ==============================================================================
PUBLISH_URL=https://your-artifactory.com/artifactory/moderne-ingest/

# For Maven/Artifactory - Option 1: Username/password authentication
PUBLISH_USER=your-username
PUBLISH_PASSWORD=your-password

# For Maven/Artifactory - Option 2: Token authentication (uncomment to use instead of user/password)
# PUBLISH_TOKEN=your-jfrog-api-token

# Optional: Moderne platform configuration
# MODERNE_TENANT=https://app.moderne.io
# MODERNE_TOKEN=your-moderne-token

# Optional: CLI version (defaults to latest stable if not set)
# MODERNE_CLI_VERSION=3.50.0
